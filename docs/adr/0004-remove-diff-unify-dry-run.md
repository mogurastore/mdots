# diffコマンドを廃止しpush/pull --dry-runに一本化する

方向なしの `diff` は欠落をエラーにしていたが、`push --dry-run` / `pull --dry-run` は欠落を新規作成予定として報告し、両方存在時は同じ unified diff を出すため代替できる。方向なしプレビューを残す利点より、表面積と保守コストの削減を優先し、互換性を保たず一括削除すると決めた。
